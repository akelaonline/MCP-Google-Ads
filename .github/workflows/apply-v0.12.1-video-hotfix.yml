name: Apply v0.12.1 video hotfix

on:
  push:
    branches:
      - agent/v0.12.1-video-hotfix
    paths:
      - .github/workflows/apply-v0.12.1-video-hotfix.yml

permissions:
  contents: write

jobs:
  hotfix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/v0.12.1-video-hotfix
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Apply patch
        run: python scripts/apply_v0121_hotfix.py

      - name: Remove temporary patch machinery
        run: |
          rm scripts/apply_v0121_hotfix.py
          rm .github/workflows/apply-v0.12.1-video-hotfix.yml

      - name: Install and validate final tree
        run: |
          python -m pip install --upgrade pip
          pip install -e '.[dev]'
          python -m pip check
          python scripts/smoke_test.py
          ruff check src tests scripts
          pytest tests/ -q

      - name: Commit hotfix
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'fix: block legacy video writes and add Demand Gen video'
          git push origin HEAD:agent/v0.12.1-video-hotfix

# One-shot production validation. This workflow deletes itself from the final tree.
